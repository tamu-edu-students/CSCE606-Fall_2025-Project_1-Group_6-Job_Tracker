<div class="container mt-4">
  <div class="container d-flex justify-content-between align-items-center mt-4">
  <h1 class="page-title mb-0">My Job Applications</h1>
  <%= link_to "New Job Application", new_job_path, class: "btn btn-outline-primary" %>
</div>
  <hr>  
  <% if @jobs.empty? %>
    <p class="text-center">You have no job applications yet. Click the button above to add one!</p>
  <% end %>

  <%= turbo_frame_tag "jobs_table" do %>
    <%= render "jobs_table", jobs: @jobs %>
  <% end %>

  <%# This is the new button, centered at the bottom of the page %>
  <div class="text-center mt-5">
    <%= link_to "Return to Dashboard", dashboard_path, class: "btn btn-secondary" %>
  </div>
</div>